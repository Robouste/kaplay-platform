{
	"name": "kaplay-platform",
	"type": "module",
	"scripts": {
		"start": "npm run dev",
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
		"check": "tsc"
	},
	"dependencies": {
		"kaplay": "^4000.0.0-alpha.15",
		"rxjs": "^7.8.1"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"typescript": "^5.7.3",
		"vite": "^6.0.7"
	}
}
